overwrite: true
schema: "src/**/*.graphql"
generates:
  src/generated/graphql.ts:
    plugins:
      - "typescript"
      - "typescript-resolvers"
  src/generated/combined-schema.graphql:
    plugins:
      - "schema-ast"
